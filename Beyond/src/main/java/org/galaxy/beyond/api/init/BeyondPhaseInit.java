package org.galaxy.beyond.api.init;

import net.neoforged.bus.api.IEventBus;

/**
 * Phase 注册入口 —— Phase 已改为枚举，仅保留空 register() 占位。
 */
public class BeyondPhaseInit {

    public static void register(IEventBus eventBus) {}
}
